import React, { FC } from 'react';

import Container from '@components/Container';

const Courses: FC = () => {
  return (
    <section>
      <Container>
        <div>Courses</div>
      </Container>
    </section>
  );
};

export default Courses;
